apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80           # external port
      targetPort: 8080   # container port
  type: LoadBalancer     # or NodePort for local clusters
